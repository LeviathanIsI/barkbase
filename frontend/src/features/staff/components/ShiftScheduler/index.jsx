/**
 * ShiftScheduler - Main export for the shift scheduling component
 * Wraps react-big-schedule for staff shift management
 */
export { default } from './ShiftScheduler';
export { default as ShiftScheduler } from './ShiftScheduler';
export { schedulerConfig, roleColors } from './schedulerConfig';
